swagger: '2.0'
info:
  version: 0.0.1
  title: Account Summary API
  description: This is sample API being generated by cedrus-api generator
  x-ibm-name: accountSummaryInteg
host: $(catalog.host)
basePath: /account/summary
schemes:
  - https
produces:
  - application/json
paths:
  /accountSummarys:
    get:
      tags:
        - AccountSummary
      description: Gets all accountSummarys from the system that the user has access to
      operationId: getAccountSummary
      produces:
        - application/json
      x-swagger-router-controller: AccountSummary
      responses:
        '200':
          description: accountSummary response
          schema:
            type: array
            items:
              $ref: '#/definitions/accountSummary'
        '404':
          description: accountSummary response
          schema:
            type: object
        '500':
          description: accountSummary response
          schema:
            type: object
      parameters:
        - name: pageSize
          in: query
          description: Number of records returned
          type: integer
        - name: pageNumber
          in: query
          description: Page number
          type: integer
    post:
      tags:
        - AccountSummary
      description: Posts all accountSummarys from the system that the user has access to
      operationId: postAccountSummary
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: AccountSummary
      responses:
        '204':
          description: accountSummary response
          schema:
            type: array
            items:
              $ref: '#/definitions/accountSummary'
        '404':
          description: accountSummary response
          schema:
            type: object
        '500':
          description: accountSummary response
          schema:
            type: object
      parameters:
        - name: accountSummarys
          in: body
          schema:
            $ref: '#/definitions/accountSummary'
    put:
      tags:
        - AccountSummary
      description: Puts all accountSummarys from the system that the user has access to
      operationId: putAccountSummary
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: AccountSummary
      responses:
        '204':
          description: accountSummary response
          schema:
            type: array
            items:
              $ref: '#/definitions/accountSummary'
        '404':
          description: accountSummary response
          schema:
            type: object
        '500':
          description: accountSummary response
          schema:
            type: object
      parameters:
        - name: accountSummarys
          in: body
          schema:
            $ref: '#/definitions/accountSummary'
    patch:
      tags:
        - AccountSummary
      description: Patchs all accountSummarys from the system that the user has access to
      operationId: patchAccountSummary
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: AccountSummary
      responses:
        '204':
          description: accountSummary response
          schema:
            type: array
            items:
              $ref: '#/definitions/accountSummary'
        '404':
          description: accountSummary response
          schema:
            type: object
        '500':
          description: accountSummary response
          schema:
            type: object
    delete:
      tags:
        - AccountSummary
      description: Deletes all accountSummarys from the system that the user has access to
      operationId: deleteAccountSummary
      produces:
        - application/json
      consumes:
        - application/json
      x-swagger-router-controller: AccountSummary
      responses:
        '204':
          description: accountSummary response
          schema:
            type: array
            items:
              $ref: '#/definitions/accountSummary'
        '404':
          description: accountSummary response
          schema:
            type: object
        '500':
          description: accountSummary response
          schema:
            type: object
      parameters:
        - name: accountSummarys
          in: body
          schema:
            $ref: '#/definitions/accountSummary'
definitions:
  ApiError:
    type: object
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string
      fields:
        type: string
  accountSummary:
    title: accountSummary
    type: object
    properties:
      id:
        type: string
      customerId:
        type: string
      overview:
        type: string
      inboundOpportunity:
        type: string
      objective:
        type: string
      accounts:
        type: array
        items:
          type: object
          properties:
            accountNumber:
              type: string
            registeredAs:
              type: string
            type:
              type: string
            accountType:
              type: string
            address:
              type: object
              properties:
                city:
                  type: string
                state:
                  type: string
            netWorth:
              type: number
          required:
            - accountNumber
            - registeredAs
            - type
            - accountType
            - address
            - netWorth
responses:
  Standard500ErrorResponse:
    description: An unexpected error occured.
    schema:
      $ref: '#/definitions/ApiError'
  PersonDoesNotExistResponse:
    description: Person does not exist.
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  gateway: datapower-gateway
  catalogs:
    apic-dev:
      properties:
        runtime-url: $(TARGET_URL)
    sb:
      properties:
        runtime-url: 'http://localhost:8080'
  assembly:
    execute:
      - invoke:
          target-url: >-
            https://accountsummaryinteg.mybluemix.net$(request.path)$(request.search)
          tls-profile: ''
          secure-gateway: false
    catch: []
